# instance/

Dossier généré par Flask/SQLAlchemy pour stocker la base SQLite locale
(`hbntory.db`) quand `DATABASE_URL` n'est pas défini. Ne pas versionner ce fichier
en environnement de production ; recréez-le avec `python database/seed.py`.
